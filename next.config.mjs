/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // <-- ADD OR ENSURE THIS LINE IS PRESENT
  // other configurations you might have, e.g.,
  // images: { unoptimized: true },
};

module.exports = nextConfig;